## Bàn giao — Frontend Refactor Batch (CTV2-118 → CTV2-130)

### Bối cảnh
Research task CTV2-118 (executor `@claude-opus`) đã audit 8 file frontend lớn nhất, sinh ra `docs/research/frontend-refactor-strategy.md` và đề xuất 11 task follow-up (CTV2-119→129), mỗi task ≤8 file. Từ đó tôi đã tạo, gán executor/reviewer, dispatch và review 2 task đầu (Phase 1).

### Trạng thái hiện tại

| Task | Status | Executor | Ghi chú |
|---|---|---|---|
| CTV2-118 | done | @claude-opus | Research doc gốc |
| **CTV2-119** | **changes-requested** | @gemini-3.6-flash | Xem "Việc cần làm" bên dưới |
| **CTV2-120** | **done** (pass) | @gpt-5.6-luna-high | Fix 6 bug pre-scan, review pass |
| **CTV2-130** (mới) | todo, chưa dispatch | — | Tách ra từ finding review CTV2-120 |
| CTV2-121 → CTV2-129 | todo, chưa dispatch | — | Blocked theo chain phụ thuộc (xem dưới) |

### Việc cần làm ngay

1. **CTV2-119** (`projects/control-tower-v2/tasks/CTV2-119-shadcn-ui-core-primitives.md`) — reviewer `@gemini-3.1-pro-high` yêu cầu sửa: thiếu `resolve.alias '@' -> ./src` trong `frontend/vitest.config.ts`, khiến `stat-card.test.tsx` fail ngay khi chạy (`Failed to resolve import "@/lib/utils"`). Logic component (StatCard/AlertBanner/StatusBadge) đã pass hết, chỉ thiếu config này. Sau khi sửa, re-review qua `/review-order CTV2-119 --ref <commit-mới>`.

2. **CTV2-130** (`projects/control-tower-v2/tasks/CTV2-130-fix-frontend-build-zustand-resolve.md`) — build `vite build` fail với lỗi Rollup không resolve được `zustand` từ `frontend/src/lib/store.ts`, dù package có trong `package.json` và `node_modules`. Đã xác nhận lỗi này **có sẵn từ trước** (build fail y hệt ở commit cha `fcdf0c6`, trước cả CTV2-119/120) — không phải do 2 task đó gây ra, nhưng đang **chặn khả năng verify build** cho các task Phase 2+ sau này. Task chưa gán executor — cần fix trước khi dispatch CTV2-121+ nếu muốn build pass được trong review.
   - Lưu ý phụ: có phát hiện `npm`/`npm run build` trên máy này đôi khi bị wrapper route sang project khác (`pim-frontend`, Next.js) — nên build/test bằng `node ./node_modules/.bin/vite`/`tsc` trực tiếp thay vì qua `npm` script cho tới khi CTV2-130 làm rõ.

### Chain phụ thuộc còn lại (để tự dispatch tiếp)

```
CTV2-121 (useMemo) → CTV2-122 (Promise.all)
                        ├→ CTV2-123 (split ChatPanel)
                        ├→ CTV2-124 (split AgentDetail)
                        └→ CTV2-125 (split ProjectDetail)
CTV2-119 + CTV2-124 + CTV2-125 → CTV2-126 (migrate stat cards)
CTV2-119 + CTV2-124 + CTV2-125 → CTV2-127 (migrate banners/modals)
CTV2-126 + CTV2-127 → CTV2-128, CTV2-129 (test coverage)
```
Lý do chain: nhiều task sửa cùng file (`AgentDetail.tsx`, `ProjectDetail.tsx`, `Dashboard.tsx`...) — không dispatch song song 2 task đụng cùng file để tránh conflict (verifier rule `no-conflicting-tasks`).

**Executor/reviewer đã gán sẵn trong frontmatter mỗi task** theo yêu cầu của bạn:
- Task đơn giản (119, 128, 129) → `@gemini-3.6-flash`
- Task khó (120–127) → `@gpt-5.6-luna-high`
- Reviewer tất cả → `@gemini-3.1-pro-high` (profile mới tạo, chưa có track record)

### Ghi chú vận hành (đã lưu vào memory, không cần lặp lại lỗi)
- `agy --print` mặc định timeout 5 phút — task nào có `npm install`/build cần thêm `--print-timeout 20m` trở lên.
- Harness auto-mode classifier chặn spawn `--dangerously-skip-permissions`/`--dangerously-bypass-approvals-and-sandbox` trừ khi bạn nêu đích danh lệnh + cờ trong chính câu yêu cầu đó — mỗi lần đổi CLI hoặc đổi role (execute↔review) coi như hành động mới, cần xác nhận lại.
- `@gemini-3.6-flash` success_rate hiện đã giảm xuống 31% (declining) sau lần reject CTV2-119 — cân nhắc khi giao thêm task cho model này.

### File liên quan
- Research doc: `docs/research/frontend-refactor-strategy.md`
- Review sheets: `projects/control-tower-v2/reviews/CTV2-119-review.md`, `CTV2-120-review.md`
- Log đầy đủ mọi thao tác: `log.md` (search "CTV2-118" tới "CTV2-130")